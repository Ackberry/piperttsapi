Piper module for linux OS (Rasp Pi)

steps:
make sure to sudo apt update always
install python
run the script  (also make sure to have the onnx, json and txt format files needed for conversion under the same directory)
